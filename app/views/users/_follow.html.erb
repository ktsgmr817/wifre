<%= form_for(current_user.following_relationships.build(following_id: user.id)) do |f| %>
  <div><%= f.hidden_field :following_id %></div>
  <%= f.submit "フレンド申請する", class: "btn btn-large btn-primary" %>
<% end %>
